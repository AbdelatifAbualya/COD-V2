version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build commands required for this static site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []

# Note: Amplify Function configurations are managed through the Amplify CLI
# and don't need to be specified in this file
